query FFZ_GetEmoteInfo($id: ID!) {
	emote(id: $id) {
		id
		owner {
			id
			login
			displayName
		}
		setID
		text
		subscriptionProduct {
			id
			state
			owner {
				id
				login
				displayName
			}
		}
	}
}